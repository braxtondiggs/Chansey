# ────────────────────────────────
# 📧 Email Configuration (Resend)
# ────────────────────────────────
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key_here
RESEND_FROM_EMAIL=noreply@cymbit.com
FRONTEND_URL=https://cymbit.com

# ────────────────────────────────
# 🔐 Authentication & API Keys
# ────────────────────────────────

BINANCE_API_KEY=your_binance_api_key_here
BINANCE_API_SECRET=your_binance_api_secret_here

COINBASE_API_KEY=your_coinbase_pro_api_key_here
COINBASE_API_SECRET=your_coinbase_pro_api_secret_here
COINBASE_API_PASSPHRASE=your_coinbase_pro_api_passphrase_here

CHANSEY_API_KEY=your_chansey_api_key_here
CCA_API_KEY=your_cca_api_key_here
COOKIE_SECRET=your-cookie-signing-secret-at-least-32-chars

# ────────────────────────────────
# 🧠 JWT / Auth Tokens
# ────────────────────────────────
JWT_SECRET=your-jwt-secret-here-minimum-32-chars
JWT_REFRESH_SECRET=your-jwt-refresh-secret-here-min-32-chars
JWT_EXPIRATION_TIME=15m
JWT_REFRESH_EXPIRATION_TIME=7d

# ────────────────────────────────
# 🔐 Security Configuration (Optional)
# ────────────────────────────────
# Password hashing salt rounds (default: 12)
# PASSWORD_SALT_ROUNDS=12
# OTP hashing salt rounds (default: 6, lower since OTPs are short-lived)
# OTP_SALT_ROUNDS=6
# OTP expiration time in minutes (default: 10)
# OTP_EXPIRATION_MINUTES=10
# Email verification token expiration in hours (default: 24)
# EMAIL_VERIFICATION_EXPIRATION_HOURS=24
# Password reset token expiration in hours (default: 1)
# PASSWORD_RESET_EXPIRATION_HOURS=1

# ────────────────────────────────
# 🗄️ Database (PostgreSQL)
# ────────────────────────────────
DATABASE_URL=jdbc:postgresql://localhost:5432/yourdb
PGHOST=localhost
PGPORT=5432
PGDATABASE=yourdb
PGUSER=your_username
PGPASSWORD=your_password_here

# ────────────────────────────────
# 🪣 MinIO (Object Storage)
# ────────────────────────────────
MINIO_HOST=your_minio_host_here
MINIO_PORT=9000
MINIO_USE_SSL=true
MINIO_ACCESS_KEY=your_minio_access_key_here
MINIO_SECRET_KEY=your_minio_secret_key_here
MINIO_BUCKET_NAME=your_minio_bucket_name_here
MINIO_BUCKET_REGION=us-east-1
# Enable public read access for specified folders (default: false)
MINIO_ENABLE_PUBLIC_ACCESS=false
# Comma-separated list of folders to make publicly readable (default: profile-images)
MINIO_PUBLIC_FOLDERS=profile-images

# ────────────────────────────────
# 🔁 Redis (Cache)
# ────────────────────────────────
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_TLS=false
REDIS_USER=
REDIS_PASSWORD=
REDIS_URL=redis://localhost:6379

# ────────────────────────────────
# ⚙️ App Settings
# ────────────────────────────────
PORT=3000
NODE_ENV=development
DISABLE_BACKGROUND_TASKS=false

# ────────────────────────────────
# 🔒 Encryption
# ────────────────────────────────
# 64-character hex string (32 bytes) for AES-256 encryption
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# REQUIRED in production - used for encrypting sensitive strategy parameters
ENCRYPTION_KEY=

# ────────────────────────────────
# 📊 OHLC Data Sync
# ────────────────────────────────
# Enable/disable OHLC background sync tasks
OHLC_SYNC_ENABLED=true
# Cron pattern for sync task (default: EVERY_HOUR)
# OHLC_SYNC_CRON=0 * * * *
# Days to retain candle data (default: 365)
OHLC_RETENTION_DAYS=365
# Comma-separated exchange priority for fetching data
OHLC_EXCHANGE_PRIORITY=binance_us,gdax,kraken

# ────────────────────────────────
# 🧪 Backtesting
# ────────────────────────────────
BACKTEST_HISTORICAL_QUEUE=backtest-historical
BACKTEST_REPLAY_QUEUE=backtest-replay
BACKTEST_TELEMETRY_STREAM=backtest-telemetry
BACKTEST_HISTORICAL_CONCURRENCY=4
BACKTEST_REPLAY_CONCURRENCY=2

# ────────────────────────────────
# 📊 Observability (Grafana Stack)
# ────────────────────────────────
# Loki - Log aggregation (optional)
# Set LOKI_ENDPOINT to enable log shipping to Grafana Loki
LOKI_ENDPOINT=
LOKI_USERNAME=
LOKI_PASSWORD=

# Tempo - Distributed tracing (optional)
# Set TEMPO_ENDPOINT to enable OpenTelemetry tracing
TEMPO_ENDPOINT=
TEMPO_AUTH_HEADER=

# OpenTelemetry service name
OTEL_SERVICE_NAME=chansey-api

# Prometheus - Application metrics
# Metrics are always exposed at /api/metrics - configure Prometheus to scrape this endpoint
# To use with Grafana Cloud Prometheus, configure remote_write in your Prometheus config
# or use the Grafana Agent to scrape and forward metrics
PROMETHEUS_METRICS_PATH=/metrics
